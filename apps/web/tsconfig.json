{
  // ルートの tsconfig.json を継承
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Next.js の TypeScript プラグイン（型安全なルーティング等を提供）
    "plugins": [{ "name": "next" }],

    // Next.js が独自に JSX を処理するため preserve を指定
    "jsx": "preserve",

    // インクリメンタルコンパイル（.tsbuildinfo を使用してビルドを高速化）
    "incremental": true,

    // パスエイリアス: @/ で app/ ディレクトリを参照可能にする
    // 例: import Foo from "@/components/Foo"
    "paths": {
      "@/*": ["./app/*"],
      "@ui/*": ["../../packages/ui/src/*"]
    }
  },
  // コンパイル対象のファイル
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
